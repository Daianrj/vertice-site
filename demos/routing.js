import {clone,esc} from './shared.js';
const initial={drivers:[{id:'M01',name:'Carlos Souza',route:'Oeste 01'},{id:'M02',name:'Marcos Lima',route:'Zona Sul 02'}],routes:{
'Oeste 01':[{seq:1,nf:'45821',client:'Mercado Horizonte',city:'Bangu',km:6.8,status:'Pendente'},{seq:2,nf:'45834',client:'Drogaria Central',city:'Campo Grande',km:11.4,status:'Pendente'},{seq:3,nf:'45842',client:'Casa Popular',city:'Santa Cruz',km:14.2,status:'Pendente'}],
'Zona Sul 02':[{seq:1,nf:'45901',client:'Loja Atlântico',city:'Botafogo',km:4.1,status:'Em rota'},{seq:2,nf:'45911',client:'Mercado Praia',city:'Copacabana',km:5.7,status:'Pendente'}]
}};let state=clone(initial),rootRef=null,driver='M01';
const render=()=>{const d=state.drivers.find(x=>x.id===driver),list=state.routes[d.route],km=list.reduce((a,x)=>a+x.km,0);rootRef.innerHTML=`<div class="miniapp"><div class="miniapp-shell"><div class="miniapp-top"><div class="miniapp-brand"><span class="miniapp-mark">V/</span><div>Roteirizador<small class="miniapp-muted">Planejamento operacional fictício</small></div></div><select class="mini-select" id="route-driver">${state.drivers.map(x=>`<option value="${x.id}" ${x.id===driver?'selected':''}>${esc(x.name)} · ${x.route}</option>`).join('')}</select></div><div class="mini-kpis"><div class="mini-kpi"><small>Paradas</small><strong>${list.length}</strong></div><div class="mini-kpi"><small>Distância</small><strong>${km.toFixed(1)} km</strong></div><div class="mini-kpi"><small>Em rota</small><strong>${list.filter(x=>x.status==='Em rota').length}</strong></div><div class="mini-kpi ok"><small>Motorista</small><strong style="font-size:16px">${esc(d.name.split(' ')[0])}</strong></div></div><div class="mini-grid-2"><div class="mini-panel"><strong>Sequência de entregas</strong><div style="margin-top:8px">${list.map(x=>`<div class="route-line"><span class="route-num">${String(x.seq).padStart(2,'0')}</span><div><strong>NF ${x.nf} · ${esc(x.client)}</strong><div class="miniapp-muted">${esc(x.city)} · ${x.km} km</div></div><button class="mini-btn" data-route-status="${x.nf}">${x.status}</button></div>`).join('')}</div></div><div class="mini-panel"><strong>Mapa operacional simplificado</strong><div style="position:relative;height:300px;margin-top:10px;border-radius:12px;background:linear-gradient(135deg,#e9eef6,#f8fafc);overflow:hidden">${list.map((x,i)=>`<span style="position:absolute;left:${16+i*27}%;top:${24+(i%2)*32}%;display:grid;place-items:center;width:34px;height:34px;border-radius:50%;background:#0b1424;color:#c9ff4a;font-size:10px;font-weight:900;box-shadow:0 8px 20px rgba(11,18,32,.2)">${x.seq}</span>`).join('')}<div style="position:absolute;left:12%;right:12%;top:50%;height:2px;background:linear-gradient(90deg,#3157ff,#c9ff4a);transform:rotate(-7deg)"></div></div></div></div></div></div>`;bind()};
const bind=()=>{document.getElementById('route-driver')?.addEventListener('change',e=>{driver=e.target.value;render()});rootRef.querySelectorAll('[data-route-status]').forEach(b=>b.addEventListener('click',()=>{const d=state.drivers.find(x=>x.id===driver),x=state.routes[d.route].find(y=>y.nf===b.dataset.routeStatus);x.status=x.status==='Pendente'?'Em rota':x.status==='Em rota'?'Entregue':'Pendente';render()}))};
export async function mount(root){rootRef=root;render();return{reset(){state=clone(initial);driver='M01';render()},destroy(){rootRef=null}}}
